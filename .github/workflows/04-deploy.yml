# This workflow completes the story by delivering the saved package.
# It starts when a pull request is merged into main.
name: 04 Deploy Workflow
on:
  pull_request:
    types: [closed]
    branches: [main]

env:
  IMAGE_NAME: tiny-health-app
  IMAGE_TAR: tiny-health-app.tar
  IMAGE_METADATA: image-metadata.env
  ARTIFACT_NAME: tiny-health-app-image
  CONTAINER_NAME: tiny-health-container
  APP_PORT: "8000"
permissions:
  # Read access is enough because this workflow only downloads artifacts and reads repo data.
  actions: read
  contents: read
jobs:
    
  deploy-package:
    # Continue only when the build workflow succeeded.
    # if: ${{ github.event.workflow_run.conclusion == 'success' }}
    if: ${{ github.event.pull_request.merged == true }}
    # Use a fresh runner for the deployment simulation.
    runs-on: ubuntu-latest
    steps:
      - name: Find latest successful build run
        # Look up the most recent successful run of the build workflow,
        # since this workflow no longer triggers directly off it.
        id: find-run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          RUN_ID="$(gh run list \
            --workflow "03-build.yml" \
            --status success \
            --branch main \
            --limit 1 \
            --json databaseId \
            --jq '.[0].databaseId')"
          if [ -z "$RUN_ID" ]; then
            echo "No successful build run found on main."
            exit 1
          fi
          echo "run_id=$RUN_ID" >> "$GITHUB_OUTPUT"

    
      - name: Download build artifact
        # Download the artifact from the located build run.
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: ${{ steps.find-run.outputs.run_id }}
          path: downloaded-artifact

      - name: Show downloaded files
        run: ls -R downloaded-artifact

      - name: Load Docker image
        run: docker load --input "downloaded-artifact/${IMAGE_TAR}"

      - name: Read image metadata from build artifact
        id: image-metadata
        run: |
          source "downloaded-artifact/${IMAGE_METADATA}"
          echo "image_tag=${IMAGE_TAG}" >> "$GITHUB_OUTPUT"
          echo "image_reference=${IMAGE_REFERENCE}" >> "$GITHUB_OUTPUT"

      - name: Show exact image reference
        run: echo "Deploying ${{ steps.image-metadata.outputs.image_reference }}"

      - name: Start container
        run: |
          docker run -d \
            --name "${CONTAINER_NAME}" \
            -p "${APP_PORT}:8000" \
            "${{ steps.image-metadata.outputs.image_reference }}"

      - name: Show running containers
        run: docker ps

      - name: Smoke test deployed app and show health response
        run: |
          for attempt in 1 2 3 4 5; do
            response="$(curl --fail --silent "http://127.0.0.1:${APP_PORT}/health")"
            if [ $? -eq 0 ]; then
              echo "Health response: ${response}"
              exit 0
            fi
            echo "Waiting for app to become ready..."
            sleep 2
          done
          echo "App did not become ready in time."
          exit 1

      - name: Stop container
        if: always()
        run: docker rm -f "${CONTAINER_NAME}" || true
